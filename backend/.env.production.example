# IMPORTANTE: Este es un archivo de ejemplo. Configura estas variables en tu plataforma de hosting (Render, Heroku, etc.)

# Database (Obtener de Neon.tech o tu proveedor de PostgreSQL)
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Security - CRÍTICO: Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="[GENERAR-VALOR-ÚNICO-64-CARACTERES]"
JWT_EXPIRES_IN="7d"

# CORS - Especificar dominio real de frontend (NO usar '*')
# Para múltiples orígenes, separar con comas: "https://app1.com,https://app2.com"
CORS_ORIGIN="https://tu-app.vercel.app"

# Server
NODE_ENV=production
PORT=3000

# GPS Accuracy (metros) - Tolerancia de señal GPS
GPS_ACCURACY_THRESHOLD_M=50

# Logging
LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_MAX=5
RATE_LIMIT_WINDOW_MS=60000
